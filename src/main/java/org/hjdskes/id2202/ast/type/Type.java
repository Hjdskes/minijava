package org.hjdskes.id2202.ast.type;

import org.hjdskes.id2202.ast.Visitable;

/**
 * Type is the common interface for all types in the MiniJava programming language.
 * Currently, only boolean, integer and integer array types are implemented.
 */
public interface Type extends Visitable {
}
